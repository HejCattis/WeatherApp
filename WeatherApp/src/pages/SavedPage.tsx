import Header from "../components/Header"

function SavedPage() {
return (
    <div>
        <Header title="Saved location" text="The locations is only saved in the session"></Header>
    </div>

)
}

export default SavedPage